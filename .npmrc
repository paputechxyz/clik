# electron-builder packages whatever it finds under node_modules and unpacks
# node_modules/node-pty out of the asar. pnpm's default isolated store links
# that path through .pnpm/, which electron-builder does not follow correctly for
# native modules, so use a flat node_modules layout.
node-linker=hoisted
